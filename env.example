# Copy to `.env.local` to override defaults locally.
# Vite only exposes variables prefixed with `VITE_`.

# Force the in-browser mock API (even when not running `vite --mode mock`)
# VITE_API_MOCK=true

# Add artificial latency (in ms) when the mock API is enabled
# VITE_API_MOCK_DELAY_MS=150


